#include <stdio.h>

int main() {
    int menu;
    char temp;
    if (scanf("%d%c", &menu, &temp) != 2 || ((temp != ' ') && (temp != '\n')))
        printf("n/a");
} else {
    while (menu != -1) {
        switch (menu) {
            case  1
        }
    }
}